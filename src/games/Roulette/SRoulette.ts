import styled from 'styled-components'

export const RouletteWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 1.5rem 0;

  & > :first-child {
    flex-grow: 3;
  }
`
